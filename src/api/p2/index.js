import { post, get } from '../request';

export default {
    // xxx
    zzz(params) {
        return post('/xxxxxxxxx/zzz',params)
    },
    // yyy
    zzzz(params) {
        return get('/yyyyyyyyy/zzzz',params)
    },
}