import { post, get } from '../request';

export default {
    // xxx
    xxx(params) {
        return post('/xxxxxxxxx/xxx',params)
    },
    // yyy
    yyy(params) {
        return get('/yyyyyyyyy/yyy',params)
    },
}